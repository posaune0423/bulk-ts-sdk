/** Default HTTP endpoint for the Bulk Exchange API production environment. */
export const BULK_DEFAULT_HTTP_URL = 'https://exchange-api.bulk.trade/api/v1'

/** Default WebSocket endpoint for the Bulk Exchange real-time API production environment. */
export const BULK_DEFAULT_WS_URL = 'wss://exchange-ws1.bulk.trade'

/** Default timeout for HTTP requests in milliseconds. */
export const DEFAULT_TIMEOUT_MS = 10_000

/** Default timeout for WebSocket operations in milliseconds. */
export const DEFAULT_WS_POST_TIMEOUT_MS = 10_000
